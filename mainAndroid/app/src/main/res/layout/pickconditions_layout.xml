<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="150dp">

    <ListView
        android:id="@+id/conditionList"
        android:layout_width="241dp"
        android:layout_height="102dp"
        android:textColor="@color/blue"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/cancelBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:backgroundTint="@color/blue"
        android:text="@string/cancel2"
        app:layout_constraintEnd_toStartOf="@+id/submitBtn"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="@+id/conditionList"
        app:layout_constraintTop_toBottomOf="@+id/conditionList" />

    <Button
        android:id="@+id/submitBtn"
        android:layout_width="wrap_content"
        android:backgroundTint="@color/blue"
        android:layout_height="wrap_content"
        android:layout_marginEnd="4dp"
        android:text="@string/submit"
        app:layout_constraintEnd_toEndOf="@+id/conditionList"
        app:layout_constraintTop_toBottomOf="@+id/conditionList" />
</androidx.constraintlayout.widget.ConstraintLayout>